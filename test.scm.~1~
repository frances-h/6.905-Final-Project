;;;

(load "model.scm")
(load "layers.scm")
(load "utils.scm")


(define net (make-model (list
						(make-fully-connected 1 3 #t)
						(make-fully-connected 3 1 #f)
						)))

(define examples (list 1 2 3 4 5 6 ))
(define labels (list 1 -1 -3 -5 -7 -9 )) ; -2n + 3

(map (lambda x (net \forward x)) inputs)