;;;

(load "model.scm")
(load "layers.scm")
(load "utils.scm")
(load "loss_fns.scm")


(define net (make-model (list
			 (make-fully-connected 1 3 #t)
			 (make-fully-connected 3 1 #f)
			 )))

(net `forward (vector (vector 3)))


(net(car (list #( #( 3))))